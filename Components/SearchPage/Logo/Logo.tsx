import React from 'react';
import classes from "./Logo.module.scss"

const Logo = () => {
    return (
        <>
            <a className={classes.logo} href="#">pinktada</a>
        </>
    );
};

export default Logo;